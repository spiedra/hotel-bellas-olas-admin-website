import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#00253c'
    },
    secondary: {
      main: '#FF2511'
    },
    background: {
      default: '#fff',
      paper: '#fff'
    },
    info: {
      main: '#FEDE00'
    }
  }
})
